<%= form_for @barcore do |f| %>
  <% if @barcore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@barcore.errors.count, "error") %> prohibited
        this barcore from being saved:</h2>
      <ul>
        <% @barcore.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
